if ($(window).width() > 992) {

   // set padding
   $('.page').css('padding-top', $('.navbar').outerHeight());
}
else {

   // set padding
   $('.page').css('padding-bottom', $('.navbar').outerHeight());
}
